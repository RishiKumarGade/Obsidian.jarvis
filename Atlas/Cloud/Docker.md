
- Docker uses a client-server architecture.


##### Docker File
`RUN` runs when docker build
`CMD` when docker run
`WORKDIR` is for changing directories inside the container and auto creates directories if not there
it is not for changing directories in local

`COPY` `localpath` `container path` auto creates if doesnt exist


